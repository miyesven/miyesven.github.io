# frozen_string_literal: true

source "https://rubygems.org"
gem 'github-pages'

#This is the theme chosen:
gem 'jekyll-paginate'
gem "jekyll-remote-theme"
# git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
